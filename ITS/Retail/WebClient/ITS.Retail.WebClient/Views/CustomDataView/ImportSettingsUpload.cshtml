@using (Html.BeginForm("ImportSettingsUploadForm", "CustomDataView", FormMethod.Post, new { id = "UploadForm", enctype = "multipart/form-data" }))
{
    ViewContext.Writer.Write("<div class='UploadForm'>");
    Html.DevExpress().UploadControl(settings =>
    {
        settings.UploadMode = UploadControlUploadMode.Standard;
        settings.Name = "UploadControl";
        settings.CallbackRouteValues = new { Controller = "CustomDataView", Action = "UploadControl" };
        settings.ShowProgressPanel = true;
        //settings.ValidationSettings.Assign(ITS.Retail.WebClient.Controllers.CustomDataViewController.UploadControlValidationSettings);
        settings.ClientSideEvents.FileUploadComplete = "CustomDataView.onFileUploadComplete_UploadControl";
    }).Render();

    ViewContext.Writer.Write("</div>");

}
